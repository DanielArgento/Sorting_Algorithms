#include <iostream>
#include "Sorting.h"

int main()
{
	//These will check out a single algorithm, don't really need these after creating Test()

	//int array[5] = { 5, 16, 8, 12, 1 };
	
	//Sorting::SelectionSort(array, 5);
	//Sorting::BubbleSort(array, 5);
	//Sorting::MergeSortExecutor(array, 5);
	//Sorting::QuickSortExecutor(array, 5);
	//Sorting::QuickSortExecutor_Optimized(array, 5);
	//Sorting::Print(array, 5);

	Sorting::Test();
	

	std::cin.get();
}